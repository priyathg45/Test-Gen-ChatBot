# Backend API base URL. Leave unset in development to use the dev server proxy (recommended).
# Set only if backend runs elsewhere, e.g. REACT_APP_CHAT_API_URL=http://localhost:5000
# REACT_APP_CHAT_API_URL=
